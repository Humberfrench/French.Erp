@using French.Erp.Application.DataObject;
@model ModelBasic<BancoDto>
@{
    ViewData["Title"] = "Bancos";
    Layout = "~/Views/Shared/_LayoutNew.cshtml";
    ViewData["User"] = Model.Nome;
    ViewData["Admin"] = Model.Admin;
    ViewData["Role"] = Model.Role;
}
<div class="text-center">
    <h3 class="display-4">Bancos</h3>
</div>
<div class="text-right">
    <button type="button" class="btn btn-success" onclick="Servico.Novo();"><i class="fa fa-file"></i>&nbsp;Novo</button>
</div>
<br />
<div style="overflow: visible;">
    <table class="table table-striped table-hover table-responsive dataTable" data-item-reload="true" id="tableDados">
        <thead class="bg-dark">
            <tr>
                <th scope="col" style="width:10%">#</th>
                <th scope="col" style="width:10%">Código</th>
                <th scope="col" style="width:60%">Nome</th>
                <th scope="col" style="width:10%">Status</th>
                <th scope="col" class="text-right" style="width:10%">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Lista)
            {
                <tr scope="row">
                    <td>@item.BancoId</td>
                    <td>@item.Codigo</td>
                    <td>@item.Nome</td>
                    <td>
                        @if (item.Status)
                        {
                            <button type="button" class="btn btn-success" title="Desativar">
                                <i class="fa fa-check"></i>
                            </button>
                        }
                        else
                        {
                            <button type="button" class="btn btn-danger" title="Ativar">
                                <i class="fa fa-ban"></i>
                            </button>
                        }
                    </td>
                    <td class="text-right">
                        <button type="button" class="btn btn-success" onclick="Servico.Edit('@item.BancoId', '@item.Nome','@item.Nome')"><i class="fa fa-pencil"></i></button>
                        <button type="button" class="btn btn-danger" onclick="Servico.Excluir('@item.BancoId')"><i class="fa fa-trash-o"></i></button>
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>
<partial name="Edit" />

@section scripts
{
    <script src="~/Views/Servico/main.js"></script>
}
